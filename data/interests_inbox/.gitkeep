这个文件的唯一作用是让 git 保留这个空目录。

不要删除它。工作流 update.yml 的提交步骤里有 `git add data/interests_inbox
data/pivot_inbox`,而 git 不跟踪空目录 —— 目录一旦从仓库里消失,
`git add` 就会 fatal: pathspec did not match,整个提交步骤失败,
当天抓好的数据全部丢弃(2026-08-18/19 真实发生过,连续两天静默丢数据)。
